server:
  port: 8084
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/alexdavid723/microservices_sisventas
          search-paths: config-data
          username: alexdavid723
          password: sandia827
          default-label: main
  security:
    user:
      name: root
      password: s3cr3t
